# Основные библиотеки для бота
python-telegram-bot==20.0  # Главная библиотека для работы с Telegram Bot API
python-dotenv==1.0.0       # Для загрузки переменных окружения из .env

# Работа с PostgreSQL
psycopg2-binary==2.9.9     # Драйвер PostgreSQL (оптимизированная версия)
asyncpg==0.28.0            # Асинхронный драйвер (если используете asyncio)
sqlalchemy==2.0.25         # ORM для работы с БД (поддержка async)

# Дополнительные утилиты
aiohttp==3.9.1             # Асинхронные HTTP-запросы (может понадобиться для API)
aiogram==3.0.0b7           # Альтернатива python-telegram-bot (если решите перейти)

# Логирование и отладка
loguru==0.7.2              # Удобное логирование (проще стандартного logging)

# Для разработки (необязательно, можно вынести в dev-requirements.txt)
pytest==7.4.0             # Тестирование
black==23.9.1             # Форматирование кода
python-decouple==3.8       # Альтернатива python-dotenv